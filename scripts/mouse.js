(function (context, $) {

    /**
     * This is basically an enum that represents mouse buttons.
     */
    var mouseButtons = {
        none: -1,
        left: 0,
        right: 2
    };

    context.mouseButtons = mouseButtons;

})(window, jQuery);